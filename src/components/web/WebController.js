import React from 'react'

const WebController = (nodeData) => {
    let nodes = nodeData
    // console.log(nodes)

    return (
        <div>
            <ul>

            </ul>
        </div>
    )
}

export default WebController